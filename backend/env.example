# CSV fayl nomi (ixtiyoriy, default: responses.csv)
CSV_FILE_NAME=responses.csv

# Server port (ixtiyoriy, default: 8000)
PORT=8000

# CORS allowed origins (production'da o'zgartirish kerak)
# ALLOWED_ORIGINS=https://your-domain.com,https://telegram-web-app.com
